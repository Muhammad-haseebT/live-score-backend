# App Name
spring.application.name=backend

# Server config
server.port=${PORT:7860}
server.address=0.0.0.0

# Datasource (Neon DB)
spring.datasource.url=jdbc:postgresql://ep-icy-river-a1sgqs4a-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_rkFXOe0x5INZ
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update    # HF pe existing schema hai, create nahi
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
